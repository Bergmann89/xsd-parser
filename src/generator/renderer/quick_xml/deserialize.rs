use crate::generator::{
    data::{ComplexType, DynamicType, EnumerationType, ReferenceType, UnionType},
    Context,
};

/* UnionType */

impl UnionType<'_> {
    pub(crate) fn render_deserializer(&self, ctx: &Context<'_, '_>) {
        let _self = self;
        let _ctx = ctx;
    }
}

/* DynamicType */

impl DynamicType<'_> {
    pub(crate) fn render_deserializer(&self, ctx: &Context<'_, '_>) {
        let _self = self;
        let _ctx = ctx;
    }
}

/* ReferenceType */

impl ReferenceType<'_> {
    pub(crate) fn render_deserializer(&self, ctx: &Context<'_, '_>) {
        let _self = self;
        let _ctx = ctx;
    }
}

/* EnumerationType */

impl EnumerationType<'_> {
    pub(crate) fn render_deserializer(&self, ctx: &Context<'_, '_>) {
        let _self = self;
        let _ctx = ctx;
    }
}

/* ComplexType */

impl ComplexType<'_> {
    pub(crate) fn render_deserializer(&self, ctx: &Context<'_, '_>) {
        let _self = self;
        let _ctx = ctx;
    }
}
