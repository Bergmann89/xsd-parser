#[derive(Debug, Clone)]
pub struct MyAllType {
    pub a: i32,
    pub b: String,
}
#[derive(Debug, Clone)]
pub struct MyChoiceType {
    pub a: Option<i32>,
    pub b: Option<String>,
}
#[derive(Debug, Clone)]
pub struct MySequenceType {
    pub a: i32,
    pub b: String,
}
